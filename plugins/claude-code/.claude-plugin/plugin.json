{
  "name": "baron",
  "version": "0.0.0",
  "description": "Platform-agnostic work orchestration (issues, scm, ci, deploy, notify) for AI coding agents: drives work tracking, source control, pipelines, and notifications across providers via the Baron MCP server, with packaged workflows surfaced as recipe skills.",
  "author": { "name": "Baron contributors" },
  "license": "Apache-2.0",
  "mcpServers": {
    "baron": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@lonca/baron-mcp-server@latest"],
      "env": {}
    }
  }
}
